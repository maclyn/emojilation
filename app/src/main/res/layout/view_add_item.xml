<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
				android:layout_width="match_parent"
				android:layout_height="wrap_content"
				android:background="?android:attr/selectableItemBackground"
				android:padding="8dp">

	<FrameLayout
		android:id="@+id/add_guard"
		android:background="?android:attr/selectableItemBackground"
		android:layout_centerInParent="true"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:paddingTop="4dp"
		android:paddingBottom="4dp"
		android:visibility="invisible">

		<LinearLayout
			android:orientation="horizontal"
			android:layout_gravity="center"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content">

			<TextView
				android:layout_width="wrap_content"
				android:layout_height="wrap_content"
				android:layout_gravity="center_vertical"
				android:ellipsize="marquee"
				android:gravity="center"
				android:text="@string/add_another"
				android:layout_marginRight="4dp"
				android:textColor="@color/textColorPrimary"
				android:textSize="16sp"/>

			<ImageView
				android:layout_gravity="center_vertical"
				android:id="@+id/remove_content"
				android:layout_width="36dp"
				android:layout_height="36dp"
				android:src="@drawable/ic_add_circle_black_36dp"/>
		</LinearLayout>
	</FrameLayout>

	<LinearLayout
		android:id="@+id/add_view"
		android:orientation="horizontal"
		android:layout_width="match_parent"
		android:layout_height="wrap_content"
		android:visibility="invisible">

		<EditText
			android:layout_weight="1"
			android:layout_gravity="center_vertical"
			android:id="@+id/content_entry"
			android:maxLines="1"
			android:ellipsize="end"
			android:inputType="text"
			android:hint="@string/nothing_yet"
			android:imeActionId="@+id/input_entry"
			android:imeActionLabel="@string/enter_entry"
			android:layout_width="match_parent"
			android:layout_height="wrap_content" >

			<requestFocus />
		</EditText>

		<Button
			android:layout_weight="2"
			android:layout_gravity="center_vertical"
			android:id="@+id/add_content_entry"
			android:layout_width="match_parent"
			android:text="@string/add"
			android:layout_height="wrap_content"/>
	</LinearLayout>
</RelativeLayout>